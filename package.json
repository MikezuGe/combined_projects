{
  "name": "combined_projects",
  "version": "1.0.13",
  "description": "This is a repository of multiple webpages.\r Each webpage has its own folder in the root folder.\r Each webpage's folder has an index file that will be served when a folder is accessed",
  "main": "index.js",
  "config": {
    "defveract": "patch"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod:test": "run-s deploy:build prod:node",
    "prod:node": "cross-env NODE_ENV=development node index",
    "start": "cross-env NODE_ENV=development run-p start:*",
    "start:nodemon": "nodemon --watch index.js --watch api --watch utility --ignore public index.js",
    "start:webpack": "webpack --watch",
    "deploy": "run-s deploy:branch deploy:bumb deploy:build deploy:amend deploy:return",
    "deploy:branch": "git checkout master && git merge dev",
    "deploy:bumb": "cross-var npm version $npm_package_config_defveract",
    "deploy:build": "cross-env NODE_ENV=production webpack",
    "deploy:amend": "git add . && git commit --amend --no-edit && git push",
    "deploy:return": "git checkout dev && git merge master && git push",
    "deploy:reverse": "git reset HEAD~ --hard && git push -f && git checkout master && git reset HEAD~ --hard && git push -f && git checkout dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "0.18.0",
    "dotenv": "7.0.0",
    "express": "4.16.4",
    "express-graphql": "0.7.1",
    "graphql": "14.1.1",
    "jsonwebtoken": "^8.5.0",
    "mongoose": "5.4.19",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-router-dom": "5.0.0",
    "socket.io": "2.2.0",
    "styled-components": "4.1.3"
  },
  "devDependencies": {
    "@babel/core": "7.4.0",
    "@babel/plugin-proposal-class-properties": "7.4.0",
    "@babel/preset-env": "7.4.2",
    "@babel/preset-react": "7.0.0",
    "babel-eslint": "10.0.1",
    "babel-loader": "8.0.5",
    "cross-env": "5.2.0",
    "cross-var": "1.1.0",
    "eslint": "5.15.3",
    "eslint-loader": "2.1.2",
    "eslint-plugin-react": "7.12.4",
    "html-webpack-plugin": "4.0.0-beta.2",
    "mongodb-memory-server": "4.2.1",
    "nodemon": "1.18.10",
    "npm-run-all": "4.1.5",
    "open-browser-webpack-plugin": "0.0.5",
    "webpack": "4.29.6",
    "webpack-cli": "3.3.0",
    "webpack-livereload-plugin": "2.2.0"
  }
}
