import React from 'react';

import { GraphDesktop, } from '../pages';


const Graph = () => {

  return (
    <GraphDesktop>
      {() => (
        <div>{'graph'}</div>
      )}
    </GraphDesktop>
  );
};


export default Graph;
